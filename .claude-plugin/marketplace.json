{
  "name": "passwd",
  "owner": {
    "name": "passwd.team"
  },
  "plugins": [
    {
      "name": "passwd",
      "source": "./packages/passwd-plugin",
      "description": "Team password manager — agent-safe MCP tools with redacted credentials, plus a skill for secure credential injection via exec --inject."
    }
  ]
}
