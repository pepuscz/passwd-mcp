{
  "name": "passwd",
  "version": "1.4.6",
  "description": "passwd.team for AI agents — browse credentials, generate TOTP codes, and inject secrets into commands without seeing raw values. Run /passwd:login after installing.",
  "author": {
    "name": "passwd.team"
  }
}
